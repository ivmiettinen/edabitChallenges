const isItChristmas = (date) => {
    console.log('date', date)

    // console.log('(date.getMonth', date.getMonth)

    // console.log('(date.getDate', date.getDate)

    return date.getMonth() === 11 && date.getDate() == 24
}
